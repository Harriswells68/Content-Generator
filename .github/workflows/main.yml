name: SeleniumBase UC+CDP

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip wheel setuptools
          pip install seleniumbase

      - name: Install Chrome
        run: sudo apt install google-chrome-stable

      - name: Install x11
        run: sudo apt install x11-apps

      - name: Run script
        run: export DISPLAY=3.82.56.61:0.0 && python new_recording_SB.py

      - name: Commit screenshots to repo
        if: always()
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add screenshots/
          git diff --staged --quiet || git commit -m "Screenshots - $(date +'%Y-%m-%d %H:%M:%S')"
          git push
